<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.polaris.he.lipstick.dao.GoodsDao">

    <select id="getByCode" resultType="com.polaris.he.lipstick.dao.object.GoodsDO">
        select
            type as type,
            brand_code as brandCode,
            goods_code as goodsCode,
            goods_name as goodsName,
            illustration as illustration,
            url as url
        from goods
        where type=#{type,jdbcType=VARCHAR} and goods_code=#{code,jdbcType=VARCHAR}
    </select>
</mapper>